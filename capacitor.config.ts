import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.magicbutton.app',
  appName: 'magic button',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
